@model IEnumerable<BEUCrtProyectoDavid.CuerpoFactura>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CabezaFactura.cbf_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Carrito.car_tipo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CabezaFactura.cbf_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Carrito.car_tipo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.crf_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.crf_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.crf_id })
        </td>
    </tr>
}

</table>
